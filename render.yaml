# BioBes ERP — Render Static Site
services:
  - type: web
    name: biobes-erp
    runtime: static
    buildCommand: echo "BioBes static frontend - no build required"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
